package com.nicefontaine;

import processing.awt.PSurfaceAWT;
import processing.core.PSurface;

import javax.swing.*;
import java.awt.*;

public class Main {

    public static void main(String... args) {
        /*
        Processor processor = new Processor();
        PSurface surface = processor.getSurface();
        surface.setSize(600, 600);
        PSurfaceAWT.SmoothCanvas canvas = (PSurfaceAWT.SmoothCanvas) surface.getNative();

        Processor processor2 = new Processor();
        PSurface surface2 = processor2.getSurface();
        surface2.setSize(600, 600);
        PSurfaceAWT.SmoothCanvas canvas2 = (PSurfaceAWT.SmoothCanvas) surface2.getNative();

        JFrame frame = new JFrame("Animation");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        JPanel content = new JPanel();
        content.setLayout(new GridBagLayout());

        frame.setContentPane(content);

        content.add(canvas);
        content.add(canvas2);

        //frame.add(canvas);
        frame.setSize(1200, 1200);
        frame.setVisible(true);

        surface.startThread();
        */
    }
}
